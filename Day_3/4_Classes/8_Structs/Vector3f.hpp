#ifndef VECTOR3F_HPP
#define VECTOR3F_HPP

// struct and class both are used for defined user defined type ,
// the difference is that class is by default private and struct is by default public

// A Vector3f data type, storing floats
struct Vector3f{

    

    //Constructor
    Vector3f();

    float x,y,z;
};

#endif